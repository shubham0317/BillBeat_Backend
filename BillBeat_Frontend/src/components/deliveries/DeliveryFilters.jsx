import { SlidersHorizontal } from 'lucide-react';

export default function DeliveryFilters({ beatId, paperBoyId, beats, paperBoys, onBeatChange, onPaperBoyChange }) {
  return <div className="grid gap-3 rounded-2xl border border-[#e6e1dd] bg-white p-4 md:grid-cols-2"><label className="flex min-h-11 items-center gap-2 rounded-xl bg-[#f7f5f2] px-3 ring-1 ring-[#e6e1dd]"><SlidersHorizontal size={16} className="text-[#8d8782]" /><select value={beatId || ''} onChange={(event) => onBeatChange(event.target.value)} className="min-w-0 flex-1 bg-transparent text-sm outline-none"><option value="">All beats</option>{beats.map((beat) => <option key={beat.id} value={beat.id}>{beat.name}</option>)}</select></label><label className="flex min-h-11 items-center gap-2 rounded-xl bg-[#f7f5f2] px-3 ring-1 ring-[#e6e1dd]"><SlidersHorizontal size={16} className="text-[#8d8782]" /><select value={paperBoyId || ''} onChange={(event) => onPaperBoyChange(event.target.value)} className="min-w-0 flex-1 bg-transparent text-sm outline-none"><option value="">All paper boys</option>{paperBoys.map((paperBoy) => <option key={paperBoy.id} value={paperBoy.id}>{paperBoy.name}</option>)}</select></label></div>;
}