import { ArrowUpRight, MapPin, Phone } from 'lucide-react';
import { Link } from 'react-router-dom';
import StatusBadge from '../common/StatusBadge';

export default function CustomerCard({ customer }) {
  return <Link to={`/customers/${customer.id}`} className="group block rounded-2xl border border-[#e6e1dd] bg-white p-5 shadow-[0_8px_24px_rgba(29,27,26,0.04)] transition hover:-translate-y-0.5 hover:border-[#f0b1ab] hover:shadow-[0_12px_30px_rgba(29,27,26,0.08)]"><div className="flex items-start justify-between gap-4"><div className="min-w-0"><h2 className="truncate font-display text-lg font-bold text-[#1d1b1a]">{customer.name}</h2><p className="mt-1 flex items-center gap-1.5 text-sm text-[#706a65]"><Phone size={14} /> {customer.mobileNumber}</p></div><ArrowUpRight size={19} className="shrink-0 text-[#a29b95] group-hover:text-[#d92d20]" /></div><p className="mt-4 flex gap-2 text-sm leading-5 text-[#706a65]"><MapPin size={16} className="mt-0.5 shrink-0" /> {customer.address}</p><div className="mt-5 flex flex-wrap gap-2"><StatusBadge tone={customer.active ? 'success' : 'neutral'}>{customer.active ? 'Active' : 'Inactive'}</StatusBadge>{customer.billStatus && <StatusBadge tone={customer.billStatus === 'PAID' ? 'success' : customer.billStatus === 'DUE' ? 'danger' : 'warning'}>{customer.billStatus}</StatusBadge>}<StatusBadge tone={customer.whatsAppEnabled ? 'success' : 'neutral'}>{customer.whatsAppEnabled ? 'WhatsApp on' : 'WhatsApp off'}</StatusBadge></div><p className="mt-4 text-xs font-semibold text-[#8d8782]">{customer.beatName || 'Beat not assigned'}</p></Link>;
}